[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "musui-thai-ai-streamer"
version = "0.1.0"
description = "Production-minded MVP scaffold for a Thai AI VTuber / streamer."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "httpx>=0.27,<1.0",
    "pydantic>=2.8,<3.0",
    "pydantic-settings>=2.3,<3.0",
    "PyYAML>=6.0,<7.0",
    "redis>=5.0,<6.0",
    "sqlalchemy>=2.0,<3.0",
    "structlog>=24.1,<25.0",
    "uvicorn[standard]>=0.30,<1.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0,<9.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["shared*", "services*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

